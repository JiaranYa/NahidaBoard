import { defineStore } from "pinia"
// import { app } from 'electron'
// const path = require('path')
// const fs = require('fs')

// const dirPath = path.join(app.getPath('documents'), 'NahidaBoard', 'data.json')

export const useStaticStore = defineStore('static', () => {
    const p = ''

    return { p }
})